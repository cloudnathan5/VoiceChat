# Database
backend/voicechat.db
*.db

# Environment variables
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build outputs. `demo/` is built by the Pages workflow on every push rather
# than committed — checking it in meant source changes shipped stale bundles
# whenever someone forgot to rebuild. Preview locally with:
#   npm run build:demo && npx serve demo
dist/
build/
demo/
frontend/dist/

# Local dev TLS certificates (needed for microphone access over the LAN).
# Generate your own; see frontend/vite.config.js.
.vite-certs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db